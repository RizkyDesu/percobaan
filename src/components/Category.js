import React, { useState } from 'react';

const Category = () => {
    const [isOpen, setIsOpen] = useState(false);
  
    return (
        <div>
            <h1>Ini Kategori</h1>
        </div>
    );
  };
  
  export default Category;