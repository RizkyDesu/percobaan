import React, { useState } from 'react';

const Footer = () => {
    const [isOpen, setIsOpen] = useState(false);
  
    return (
        <footer>
            <h1>Ini Footer</h1>
        </footer>
    );
  };
  
  export default Footer;