import React, { useState } from 'react';

const Populer = () => {
    const [isOpen, setIsOpen] = useState(false);
  
    return (
        <div>
            <h1>Ini Populer</h1>
        </div>
    );
  };
  
  export default Populer;