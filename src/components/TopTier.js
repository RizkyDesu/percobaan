import React, { useState } from 'react';

const TopTier = () => {
    const [isOpen, setIsOpen] = useState(false);
  
    return (
        <div>
            <h1>Ini Pilihan Teratas atau toptier</h1>
        </div>
    );
  };
  
  export default TopTier;